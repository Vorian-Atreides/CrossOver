#include <gtest/gtest.h>

#include "AlertWorker.h"

TEST(ShouldRaiseAnAlertSucceed, AlertWorker)
{
    // AlertWorker worker;
    // worker.shoudlRaiseAnAlert();
    // EXPECT_GT(SigarWrapper::getCpu(), 0.0);
}